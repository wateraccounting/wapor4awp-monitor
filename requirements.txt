# WaPOR4AWP Global Dashboard — Python dependencies
# Install with: pip install -r requirements.txt

# Core scientific stack
pandas>=2.0
numpy>=1.24
scipy>=1.10

# HTTP / API
requests>=2.31

# Streamlit dashboard
streamlit>=1.32
plotly>=5.20

# Word document generation (blueprint scripts only)
python-docx>=1.1

# Optional: for running GEE locally (not required for dashboard)
# earthengine-api>=0.1.390
